export default class RecoilProfile {
  vertical: number;
  horizontal: number;

  constructor(vertical: number, horizontal: number) {
    this.vertical = vertical;
    this.horizontal = horizontal;
  }
}
